import { PromisedBtnDirective } from './promised-btn.directive';

describe('PromisedBtnDirective', () => {
    it('should create an instance', () => {
        const directive = new PromisedBtnDirective();
        expect(directive).toBeTruthy();
    });
});
